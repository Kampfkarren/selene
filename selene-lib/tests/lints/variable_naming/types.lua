-- valid type (PascalCase)
local MyType = {}

-- invalid type: doesn't conform to ^[A-Z][a-zA-Z0-9]+$
local mytype = {}
local X = {}  -- single-letter X may be considered invalid with current pattern
