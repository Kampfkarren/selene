---
base: lua52
globals:
  math.log:
    args:
      - type: number
      - required: false
        type: number
  string.pack:
    args:
      - type: string
    must_use: true
  string.packsize:
    args:
      - type: string
    must_use: true
  string.unpack:
    args:
      - type: string
    must_use: true
